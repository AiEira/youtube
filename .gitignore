# Audio files only — too large for git
*.m4a
*.mp3
*.wav

# Everything else in out/ is TRACKED by git:
#   out/*.md                 — blog rewrites (keep)
#   out/transcripts/*.json   — whisper raw + clean output (keep)
#   out/README.md            — mirror table (keep)
